# Java Arrays

### Arrays are used to stroe multiple value in a single variable, instead of declaring separate variables for each value. 

### To declare an array, define the variable type with **square brackets**[]:

### Access the Elements of an Array

### Change an Array Elements

### Array Length

### The **new** Keyword
#### You can also create an array y specifying its size with **new** This make an empty array with space for  a fixednumber of elements, which you can fill later: